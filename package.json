{
  "name": "digital-logic-sim",
  "version": "1.4.1",
  "homepage": "http://hasnainroopawalla.github.io/flux",
  "description": "Digital Logic Gate Simulator using React and WebGPU",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"pnpm start:server:watch\" \"pnpm start:client\"",
    "start:client": "pnpm --filter @flux/web start",
    "start:server": "pnpm --filter @flux/server start",
    "start:server:watch": "pnpm --filter @flux/server start:watch",
    "preview": "pnpm --filter @flux/web preview",
    "build": "pnpm --filter @flux/web build --emptyOutDir",
    "test:unit": "pnpm -r run test",
    "test": "pnpm -r test",
    "lint": "biome check && tsc -p tsconfig.json --noEmit",
    "lint:fix": "biome check --fix . && biome format --write ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.20.0",
  "devDependencies": {
    "@biomejs/biome": "2.2.5",
    "@types/jest": "^30.0.0",
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.8"
  }
}
